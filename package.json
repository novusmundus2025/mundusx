{
  "name": "opengpu",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "echo \"lint not configured yet\"",
    "test": "echo \"test not configured yet\"",
    "smoke:local": "./scripts/localhost-smoke.sh"
  }
}
